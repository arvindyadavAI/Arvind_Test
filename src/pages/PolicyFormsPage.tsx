import AppLayout from '../components/AppLayout';import StepIndicator from '../components/StepIndicator';import { FORM_OPTIONS } from '../data/mockData';import { usePolicy } from '../utils/PolicyContext';import { useNavigate } from 'react-router-dom';
export default function PolicyFormsPage(){const {selectedForms,update}=usePolicy();const nav=useNavigate();const toggle=(f:string)=>update({selectedForms:selectedForms.includes(f)?selectedForms.filter((x:string)=>x!==f):[...selectedForms,f]}); return <AppLayout><StepIndicator step={3}/><h2>Policy Forms & Endorsements</h2><div className='forms'>{FORM_OPTIONS.map(f=><label key={f}><input type='checkbox' checked={selectedForms.includes(f)} onChange={()=>toggle(f)}/>{f}</label>)}</div><button className='primary' onClick={()=>nav('/pricing')}>Calculate Premium</button></AppLayout>}
